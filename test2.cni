fn main(args):
    files = {"nobin": {}}
    write(files.__class__)
    with file("nre.nobin", 'r') as f:
        write(files.__class__)
        files<"nobin"> |= {"nre":f.read()}
        write(files.__class__)
        write(files<'nobin'><'nre'>)